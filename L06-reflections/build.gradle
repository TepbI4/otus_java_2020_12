plugins {
    id 'java'
}

repositories {
    mavenCentral()
}

dependencies {
    testImplementation ('org.assertj:assertj-core')
}

compileJava {
    options.compilerArgs += ['--enable-preview']
}

compileTestJava {
    options.compilerArgs += ['--enable-preview']
}